# Lets Trip Together - Public API

API pública em .NET 8 que atua como gateway/proxy entre clientes externos e a API interna do sistema.

## Execução

```bash
dotnet run --project src/PublicApi/PublicApi.csproj
```

Acesse o Swagger em: `http://localhost:5089/swagger`